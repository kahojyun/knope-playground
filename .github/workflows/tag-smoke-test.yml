name: Tag Smoke Test

on:
  push:
    tags:
      - "v*"

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Print tag context
        run: |
          echo "ref=$GITHUB_REF"
          echo "ref_name=$GITHUB_REF_NAME"
